<!DOCTYPE html>
<html lang="{{ app.request.locale }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{{ 'title.home'|trans }}{% endblock %}</title>
    <link rel="icon" type="image/x-icon" href="{{ asset('bootstrap/assets/favicon.ico') }}">
 <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link 
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" 
        rel="stylesheet" 
        crossorigin="anonymous">

    <link 
        href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" 
        rel="stylesheet">

    <link rel="stylesheet" href="{{ asset('bootstrap/css/styles.css') }}">
    <link rel="stylesheet" href="{{ asset('bootstrap/css/modern.css') }}">
    <link rel="stylesheet" href="{{ asset('css/dark-mode.css') }}">
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Plus+Jakarta+Sans:wght@700;800&display=swap" rel="stylesheet">

    {% block stylesheets %}
    {% endblock %}
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg bg-light navbar-light shadow-sm py-3 sticky-top">
        <div class="container">
            <!-- Logo + titre -->
            <a class="navbar-brand fw-bold text-primary d-flex align-items-center" href="{{ path('main') }}" style="font-size: 1.8rem; animation: fadeIn 1.2s;">
                <i class="bi bi-pencil-square me-2"></i> Minouverse
            </a>

            <!-- Menu burger -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span> 
            </button>

            <!-- Liens de navigation -->
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto text-center">
                    <li class="nav-item">
                        <a class="nav-link fw-semibold text-secondary hover-animate" href="{{ path('main') }}">
                            <i class="bi bi-house-door-fill me-1"></i> {{ 'nav.home'|trans }}
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link fw-semibold text-secondary hover-animate" href="{{ path('post_list') }}">
                            <i class="bi bi-newspaper me-1"></i> {{ 'nav.articles'|trans }}
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link fw-semibold text-secondary hover-animate" href="{{ path('category_list') }}">
                            <i class="bi bi-tags-fill me-1"></i> {{ 'nav.categories'|trans }}
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link fw-semibold text-secondary hover-animate" href="#contact">
                            <i class="bi bi-envelope-fill me-1"></i> {{ 'nav.contact'|trans }}
                        </a>
                    </li>
                    <li class="nav-item d-flex align-items-center ms-2">
                        <button id="theme-toggle" class="theme-toggle" aria-label="Toggle dark mode" title="{{ 'nav.theme_toggle'|trans }}">
                            <span class="theme-toggle-slider">
                                <i class="bi bi-sun-fill"></i>
                            </span>
                        </button>
                    </li>
                    {% if app.user %}
                        {% if 'ROLE_ADMIN' in app.user.roles %}
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle fw-semibold text-secondary hover-animate" href="#" id="navbarDropdownAdmin" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    <i class="bi bi-gear-fill me-1"></i> {{ 'nav.admin'|trans }}
                                </a>
                                <ul class="dropdown-menu" aria-labelledby="navbarDropdownAdmin">
                                    <li><a class="dropdown-item" href="{{ path('admin_users') }}">
                                        <i class="bi bi-people me-2"></i>{{ 'nav.users'|trans }}
                                    </a></li>
                                    <li><a class="dropdown-item" href="{{ path('admin_reports') }}">
                                        <i class="bi bi-flag me-2"></i>{{ 'nav.reports'|trans }}
                                    </a></li>
                                    <li><a class="dropdown-item" href="{{ path('admin_category_suggestions') }}">
                                        <i class="bi bi-lightbulb me-2"></i>{{ 'nav.suggestions'|trans }}
                                    </a></li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item" href="{{ path('app_2fa_status') }}">
                                        <i class="bi bi-shield-lock me-2"></i>Double authentification
                                    </a></li>
                                </ul>
                            </li>
                        {% endif %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle fw-semibold text-secondary hover-animate" href="#" id="navbarDropdownUser" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="bi bi-person-circle me-1"></i> {{ app.user.email }}
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownUser">
                                <li><a class="dropdown-item" href="{{ path('favorite_list') }}">
                                    <i class="bi bi-heart-fill text-danger me-2"></i>Mes favoris
                                </a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="{{ path('app_2fa_status') }}">
                                    <i class="bi bi-shield-lock me-2"></i>{{ '2fa.menu'|trans }}
                                </a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
                                    <i class="bi bi-box-arrow-right me-2"></i>{{ 'nav.logout'|trans }}
                                </a></li>
                            </ul>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link fw-semibold text-secondary hover-animate" href="{{ path('app_login') }}">
                                <i class="bi bi-box-arrow-in-right me-1"></i> {{ 'nav.login'|trans }}
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link fw-semibold text-secondary hover-animate" href="{{ path('app_register') }}">
                                <i class="bi bi-person-plus-fill me-1"></i> {{ 'nav.register'|trans }}
                            </a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="container mt-4">
        {% block body %}
        {% endblock %}
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-light py-5 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    <h5 class="fw-bold mb-3">
                        <i class="bi bi-pencil-square me-2"></i> Minouverse
                    </h5>
                    <p class="text-muted">{{ 'footer.copyright'|trans }}</p>
                </div>
                <div class="col-md-4 text-center">
                    <h6 class="fw-bold mb-3">{{ 'footer.follow'|trans }}</h6>
                    <div class="d-flex justify-content-center gap-3">
                        <a href="https://instagram.com" target="_blank" class="text-light hover-social">
                            <i class="fab fa-instagram fs-4"></i>
                        </a>
                        <a href="https://tiktok.com" target="_blank" class="text-light hover-social">
                            <i class="fab fa-tiktok fs-4"></i>
                        </a>
                        <a href="https://github.com" target="_blank" class="text-light hover-social">
                            <i class="fab fa-github fs-4"></i>
                        </a>
                    </div>
                </div>
                <div class="col-md-4 text-md-end">
                    <h6 class="fw-bold mb-3">
                        <i class="bi bi-globe me-2"></i>{{ 'footer.language'|trans }}
                    </h6>
                    <div class="language-selector">
                        <a href="{{ path('change_locale', {'_locale': 'fr'}) }}" 
                           class="language-link {% if app.request.locale == 'fr' %}active{% endif %}"
                           title="Français">
                            🇫🇷 FR
                        </a>
                        <span class="text-muted mx-2">|</span>
                        <a href="{{ path('change_locale', {'_locale': 'en'}) }}" 
                           class="language-link {% if app.request.locale == 'en' %}active{% endif %}"
                           title="English">
                            🇬🇧 EN
                        </a>
                    </div>
                </div>
            </div>
            <!-- Section Contact -->
            <div class="row mt-4" id="contact">
                <div class="col-12 text-center">
                    <h6 class="fw-bold mb-3">{{ 'footer.contact'|trans }}</h6>
                    <p class="text-muted">
                        <i class="bi bi-envelope me-2"></i> contact@minouverse.com
                    </p>
                    <p class="text-muted">
                        <i class="bi bi-geo-alt me-2"></i> {{ 'footer.location'|trans }}
                    </p>
                </div>
            </div>
        </div>
    </footer>

    <script 
        src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" 
        crossorigin="anonymous">
    </script>
    
    <!-- Fix Dropdown Menu -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Initialize all dropdowns
            var dropdownElementList = [].slice.call(document.querySelectorAll('.dropdown-toggle'));
            var dropdownList = dropdownElementList.map(function (dropdownToggleEl) {
                return new bootstrap.Dropdown(dropdownToggleEl);
            });
        });
    </script>
    
    <!-- Modern Interactions Script -->
    <script>
        // Navbar scroll effect
        window.addEventListener('scroll', function() {
            const navbar = document.querySelector('.navbar');
            if (window.scrollY > 50) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });

        // Scroll reveal animation
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver(function(entries) {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('revealed');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.scroll-reveal').forEach(el => {
            observer.observe(el);
        });

        // Add fade-in animation to cards on page load
        document.addEventListener('DOMContentLoaded', function() {
            const cards = document.querySelectorAll('.card');
            cards.forEach((card, index) => {
                card.classList.add('fade-in');
                card.style.animationDelay = `${index * 0.1}s`;
            });
        });
    </script>
    
    <!-- Dark Mode Toggle Script -->
    <script>
        // Initialisation du thème au chargement de la page
        (function() {
            const savedTheme = localStorage.getItem('theme');
            const systemPrefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
            const initialTheme = savedTheme || (systemPrefersDark ? 'dark' : 'light');
            
            // Appliquer le thème immédiatement pour éviter le flash
            document.documentElement.setAttribute('data-theme', initialTheme);
        })();

        // Gestion du toggle après le chargement du DOM
        document.addEventListener('DOMContentLoaded', function() {
            const themeToggle = document.getElementById('theme-toggle');
            if (!themeToggle) {
                console.error('Theme toggle button not found!');
                return;
            }
            
            const htmlElement = document.documentElement;
            const themeIcon = themeToggle.querySelector('i');
            
            // Récupérer le thème actuel
            const currentTheme = htmlElement.getAttribute('data-theme');
            updateIcon(currentTheme);
            
            // Gestion du clic sur le bouton
            themeToggle.addEventListener('click', function() {
                const currentTheme = htmlElement.getAttribute('data-theme');
                const newTheme = currentTheme === 'light' ? 'dark' : 'light';
                
                htmlElement.setAttribute('data-theme', newTheme);
                localStorage.setItem('theme', newTheme);
                updateIcon(newTheme);
            });
            
            // Fonction pour mettre à jour l'icône
            function updateIcon(theme) {
                if (theme === 'dark') {
                    themeIcon.className = 'bi bi-moon-fill';
                } else {
                    themeIcon.className = 'bi bi-sun-fill';
                }
            }
            
            // Écouter les changements de préférence système
            window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', function(e) {
                if (!localStorage.getItem('theme')) {
                    const newTheme = e.matches ? 'dark' : 'light';
                    htmlElement.setAttribute('data-theme', newTheme);
                    updateIcon(newTheme);
                }
            });
        });
    </script>
    
    <style>
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .hover-animate {
            transition: color 0.3s ease, transform 0.3s ease;
        }

        .hover-animate:hover {
            color: #0d6efd;
            transform: translateY(-2px);
        }

        .hover-social {
            transition: transform 0.3s ease, color 0.3s ease;
        }

        .hover-social:hover {
            transform: scale(1.2);
            color: #0d6efd !important;
        }

        .language-selector {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .language-link {
            color: #adb5bd;
            text-decoration: none;
            font-weight: 500;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            padding: 0.25rem 0.5rem;
            border-radius: 0.25rem;
        }

        .language-link:hover {
            color: #fff;
            background-color: rgba(255, 255, 255, 0.1);
            transform: translateY(-2px);
        }

        .language-link.active {
            color: #0d6efd;
            font-weight: 700;
            text-decoration: underline;
        }
    </style>
    
    {% block javascripts %}
    {% endblock %}
</body>
</html>